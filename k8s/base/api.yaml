apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: yotowebmgr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      serviceAccountName: yotowebmgr-api
      containers:
        - name: api
          image: yotowebmgr-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: yotowebmgr-config
            - secretRef:
                name: yotowebmgr-secrets
          volumeMounts:
            - name: imports
              mountPath: /var/lib/yotowebmgr/media/imports
            - name: originals
              mountPath: /var/lib/yotowebmgr/media/originals
            - name: processed
              mountPath: /var/lib/yotowebmgr/media/processed
            - name: artwork
              mountPath: /var/lib/yotowebmgr/media/artwork
            - name: exports
              mountPath: /var/lib/yotowebmgr/media/exports
      volumes:
        - name: imports
          persistentVolumeClaim:
            claimName: imports-pvc
        - name: originals
          persistentVolumeClaim:
            claimName: originals-pvc
        - name: processed
          persistentVolumeClaim:
            claimName: processed-pvc
        - name: artwork
          persistentVolumeClaim:
            claimName: artwork-pvc
        - name: exports
          persistentVolumeClaim:
            claimName: exports-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: yotowebmgr
spec:
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
