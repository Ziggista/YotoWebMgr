apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: yotowebmgr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: yotowebmgr-worker:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: yotowebmgr-config
            - secretRef:
                name: yotowebmgr-secrets
          volumeMounts:
            - name: imports
              mountPath: /var/lib/yotowebmgr/media/imports
            - name: originals
              mountPath: /var/lib/yotowebmgr/media/originals
            - name: processed
              mountPath: /var/lib/yotowebmgr/media/processed
            - name: artwork
              mountPath: /var/lib/yotowebmgr/media/artwork
            - name: exports
              mountPath: /var/lib/yotowebmgr/media/exports
      volumes:
        - name: imports
          persistentVolumeClaim:
            claimName: imports-pvc
        - name: originals
          persistentVolumeClaim:
            claimName: originals-pvc
        - name: processed
          persistentVolumeClaim:
            claimName: processed-pvc
        - name: artwork
          persistentVolumeClaim:
            claimName: artwork-pvc
        - name: exports
          persistentVolumeClaim:
            claimName: exports-pvc
